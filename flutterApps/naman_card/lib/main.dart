import 'package:flutter/material.dart';

void main() {
  runApp(Naman());
}

class Naman extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        backgroundColor: Colors.orange,
        body: Container(),
      ),
    );
  }
}
